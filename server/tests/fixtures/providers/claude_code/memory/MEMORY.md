# Project Memory

This is the auto-managed Claude Code index (`MEMORY.md`). It is tagged as the
`memory` provider memory type by the Claude adapter (Story 2.2).

## Preferences

Prefer concise answers with code examples.

## Workflow

- Run `cargo test` before committing.
- Update the changelog for user-visible changes.
