# Rejecter fixture

This file is named `CLAUDE.md` — a human-authored instruction file that the
Claude adapter must reject by name as an `unsupported_artifact` diagnostic
(never indexed). It is here to anchor the boundary contract test.
